<%= simple_form_for(device) do |t| %>
<%= t.input :category, collection: Device::CATEGORY %>
<%= t.input :subcategory, collection: Device::SUBCATEGORY %>
<% if device.persisted?  %>
  <%= t.submit "Mettre à jour mon capteur" %>
<% else %>
  <%= t.submit "Sauver mon nouveau capteur" %>
<% end %>
<% end %>
